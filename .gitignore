# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
dist/
!deploy/prod/dist/
deploy/prod/dist/*.tar
build/
*.egg-info/
.eggs/

# Test / coverage
.coverage
htmlcov/
.pytest_cache/

# Local config overrides (not committed)
config/config.*.local.yaml
.claude/settings.local.json

# Local secrets (not committed — copy deploy/dev/.env.example to deploy/dev/.env and fill in)
.env

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Output files produced by the service (don't commit processed results)
*.txt
*.html
!config/**/*.txt
!docs/**/*.txt
!tasks/**/*.txt

# Audio for testing — track content, but keep _private untracked
audio/_private/
logs/*
!logs/.gitkeep